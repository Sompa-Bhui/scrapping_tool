# email_scarping

Email scraping tool with a web UI, backend API, and export options.

## Setup

1. Install dependencies:
   npm install
2. Start the server:
   npm start
3. Open in browser:
   http://localhost:3000/

## Notes

- Chrome session data is stored locally in chrome-session.
- Exported files are written to downloads.
